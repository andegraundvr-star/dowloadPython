[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ваш_пакет"
version = "0.1.0"
dependencies = [
    "requests>=2.25.1",
    "numpy>=1.19.5"
]